原型模式：
  就是如何快速构建对象的方法总结，
  简单工厂将getter setter 封装到
  某个方法中；通过jdk提供的clonable
  接口实现快速复制；
	
缺点：必须有克隆方法
	  
	
scope="prototype" 不通过构造器来创建对象

浅克隆：对于引用类型，克隆的是地址，
		而不是值，当原来对象数据改变，
		会使克隆后的对象数据也改变
		
深克隆（JDk本身提供）：在内存中，通过字节码读写对象重新创建，
		判断有构造器，没有readResolve方法
		就创建对象